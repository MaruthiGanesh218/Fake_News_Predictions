#!/usr/bin/env bash
# NEWS CHUNK 8 - Frontend Result Display Enhancement

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}" )" && pwd)"
PROJECT_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"
"${PROJECT_ROOT}/../scripts/verify-chunk-08.sh" "$@"
